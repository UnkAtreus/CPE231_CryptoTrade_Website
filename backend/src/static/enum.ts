export enum Gender {
  Male,
  Female,
  Other,
}
export enum TranasctionMethod {
  Deposit,
  Withdraw,
}

export enum OrderMethod {
  Sell,
  Buy,
}

export enum TransactionStatus {
  Pending,
  Done,
  Cancel,
}
